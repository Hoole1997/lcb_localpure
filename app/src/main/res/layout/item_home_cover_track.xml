<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="135dp"
    android:layout_height="wrap_content"
    android:orientation="vertical">
    <FrameLayout android:layout_width="135dp" android:layout_height="135dp">
        <com.example.lcb.app.home.HomeTrackArtworkView
            android:id="@+id/cover"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/bg_home_cover_large" />
        <ImageView android:id="@+id/play" android:layout_width="36dp" android:layout_height="36dp" android:layout_gravity="center" android:contentDescription="@string/home_play" android:src="@drawable/ic_home_play" />
    </FrameLayout>
    <TextView
        android:id="@+id/title"
        style="@style/HomeCoverTitle"
        android:layout_marginTop="7dp"
        android:ellipsize="end"
        android:maxLines="1" />

    <TextView
        android:id="@+id/artist"
        style="@style/HomeCoverArtist"
        android:layout_marginTop="2dp"
        android:ellipsize="end"
        android:maxLines="1" />
</LinearLayout>
